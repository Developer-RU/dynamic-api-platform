# --- Self-update (enabled by default in Docker Compose) ---
# Set UPDATE_EXECUTOR_ENABLED=false to disable in-app updates
# APP_VERSION=1.5.1
# UPDATE_HOST_PROJECT_ROOT is set automatically via ${PWD} in docker-compose.yml
# UPDATE_DOCKER_NETWORK=dap_default
# UPDATE_DATA_VOLUME=dap_update_data
# UPDATE_HEALTH_URL=http://backend:3001/api/health
